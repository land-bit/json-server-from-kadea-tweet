{
  "name": "twitter-clone-json-server",
  "description": "Data for the twitter clone repo",
  "version": "1.0.0",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
